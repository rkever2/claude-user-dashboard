# ============================================================
# Claude Code Usage Dashboard — Configuration
# ============================================================
# Copy this file to .env and optionally add your admin API key.
#
# Without an API key, the dashboard runs in LOCAL mode only,
# reading usage data from ~/.claude/ on this machine.
#
# With an API key, a mode switcher appears in the sidebar
# letting you toggle between Local and API mode at runtime.
# ============================================================

# --- Local data directory ------------------------------------
# Path to your ~/.claude directory. Most users don't need to
# change this. Mounted read-only inside the container.
CLAUDE_DATA_DIR=~/.claude

# --- Anthropic Admin API (optional) -------------------------
# Adding a key enables the API/Local mode switcher in the UI.
# Get your key at: https://console.anthropic.com/settings/admin-keys
# Requires the "org admin" role on your Anthropic account.
# ANTHROPIC_ADMIN_API_KEY=sk-ant-admin-...

# --- Dashboard port ------------------------------------------
PORT=3080
